# AGENTS.md

## Read first
1. `AGENTS.md` (this file)
2. `GPT_PROJECT_INSTRUCTIONS.md`

## Working rules
- Keep outputs field-specific (DARAJAT and SALAK analyzed separately where required).
- Do not invent cost drivers that are not validated by data.
- Use stepwise delivery: ingestion -> classification -> driver validation -> app.
- Prefer clear, auditable artifacts in `data/processed/` and `reports/`.
